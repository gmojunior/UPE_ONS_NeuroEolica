program Mpsusbi;

uses
  Forms,
  Mps_usb in 'MPS_USB.PAS' {MainForm},
  Sobre in 'SOBRE.PAS' {CaixaSobre};

{$R *.RES}

begin
  Application.Title := 'CONTEÚDO';
  Application.CreateForm(TMainForm, MainForm);
  Application.CreateForm(TCaixaSobre, CaixaSobre);
  Application.Run;
end.
